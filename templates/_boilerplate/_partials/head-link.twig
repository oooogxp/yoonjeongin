{# -- This is a place for any CSS <link> that needs to exist on every page on the site -- #}

{# -- Main Entry for custom JavaScript Code from /src/sass/app.scss -- #}
<link rel="stylesheet" href="{{ mix('css/app.css') }}">